{
  "name": "student-team-members-management-app",
  "private": true,
  "version": "1.0.0",
  "description": "Student Team Members Management Application",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "keywords": [
    "react",
    "express",
    "mongodb",
    "assignment"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
